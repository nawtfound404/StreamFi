{
  "scripts": {
    "compose:up": "docker compose up --build"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "express": "^5.1.0"
  }
}
